// comming soon

class Polygon {
  MAX_VERTICES = 30
  constructor(noOfVertices = 5, color = '#000') {
    this.noOfVertices = noOfVertices
    this.color = color
    this.MaxVertices = this.MAX_VERTICES
  }
  update() {}
  draw() {}
  checkIfCollided(polygon) {}
}
